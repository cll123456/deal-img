/**
 * 普通二维码的默认数据
 * @type {{margin: number, size: number, mainColor: string, text: string, subColor: string}}
 */
export const qrcodeJson = {
    text: "",
    margin: 0,
    size: 100,
    mainColor: "#000",
    subColor: "#fff",
}
